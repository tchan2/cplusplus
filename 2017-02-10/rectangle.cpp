rectangle :: get_perimeter()
{
    return ((2*width) + (2*length));
}

rectangle :: get area()
{
    return width*length;
}

rectangle :: set_width (double w)
{
    width = w;
}

rectangle :: set_length (double l)
{
    length = l;
}
